@page
@model WebApp1.Pages.Admin.SessionsModel
@{
    ViewData["Title"] = "Sessions";
}
<h2>Sessions</h2>
<div>
    <a asp-page="CreateSession">Create new session</a>
</div>
<div>
    <a asp-page="ManageLecturers">Manage lecturers</a>
</div>
<h4>Upcoming sessions</h4>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UpcomingSessions[0].Course)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UpcomingSessions[0].ClassRoom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UpcomingSessions[0].Time)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.UpcomingSessions)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Course.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ClassRoom.Number)
                </td>
                <td>
                    @item.Time.ToString("HH:mm dd.MM")
                </td>
                <td class="text-right">
                    <a asp-page="ViewSession" asp-route-id="@item.ID">Details</a> |
                    <a asp-page="DeleteSession" asp-route-id="@item.ID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<h4>Past sessions</h4>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PastSessions[0].Course)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PastSessions[0].ClassRoom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PastSessions[0].Time)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.PastSessions)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Course.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ClassRoom.Number)
                </td>
                <td>
                    @item.Time.ToString("HH:mm dd.MM")
                </td>
                <td class="text-right">
                    <a asp-page="ViewSession" asp-route-id="@item.ID">Details</a> |
                    <a asp-page="DeleteSession" asp-route-id="@item.ID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<h4>Cases</h4>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cases[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cases[0].Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Cases)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td class="text-right">
                    <a asp-page="ViewCase" asp-route-id="@item.ID">View</a>
                </td>
            </tr>
        }
    </tbody>
</table>
@page
@model WebApp1.Areas.Identity.Pages.Account.RegisterAdminModel
@{
    ViewData["Title"] = "Register";
}
@if (Model.Exp_time < DateTime.Now)
{
    <h2>The registration token has expired. Ask the system administrator for a new registration link.</h2>
}
else
{
    <div class="row">
        <div class="col-md-4">
            <form method="post">
                <h3>Create a new account</h3>
                <h5>Username: @Model.UserEmail</h5>
                <hr />
                <div asp-validation-summary="All" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="Input.Password"></label>
                    <input asp-for="Input.Password" class="form-control" />
                    <span asp-validation-for="Input.Password" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Input.ConfirmPassword"></label>
                    <input asp-for="Input.ConfirmPassword" class="form-control" />
                    <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
                </div>
                <button type="submit" class="btn btn-primary">Register</button>
            </form>
        </div>
    </div>
}


@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}


